apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-blue.fullname" . }}
  labels:
    {{- include "nginx-blue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-blue.selectorLabels" . | nindent 4 }}
